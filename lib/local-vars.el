(defvar gitlab-hosts-alist '()
  "List of hosted Gitlab instances to configure for git-link, etc")

(defvar user-company nil
  "Name of the company I'm working for on a given machine")
(defvar user-team nil
  "Name of the team I'm working for on a given machine")
