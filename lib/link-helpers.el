;; TODO Define these in local.el template
(defvar hosted-gitlab-host nil)
(defvar company-atlassian-domain nil)

(defvar atlassian-base-domain "atlassian.net")
(defvar atlassian-jira-path "browse")
(defvar github-host "https://github.com")
(defvar gitlab-host "https://gitlab.com")

;; (defvar atlassian-url-prefix
  ;; (concat "https://" company-atlassian-domain "." atlassian-domain))

;; (defun kdz/org-hosted-gitlab-link (repository)
;;   (string-join (hosted-gitlab-host repository) "/"))

;; (defun kdz/org-gitlab-link (repository)
;;   (string-join (gitlab-host repository) "/"))

;; (defun kdz/org-github-link (repository)
;;   (string-join (github-host repository) "/"))

;; (defun kdz/org-jira-link (ticket)
;;   (string-join '(atlassian-url-prefix atlassian-jira-path ticket) "/"))
